using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpaceSoundManagerScript : MonoBehaviour {
    
       







/*
    public static AudioClip space;

    static AudioSource spacesrc;

    // Use this for initialization
    void Start()
    {
        space = Resources.Load<AudioClip>("space");
     


        spacesrc = GetComponent<AudioSource>();
    }

    // Update is called once per frame
    void Update()
    {

    }

    public static void PlaySound(string clip)
    {
        switch (clip)
        {
            case "space":
                if (!spacesrc.isPlaying)
                {
                    spacesrc.PlayOneShot(space);
                }
                break;
     
        }
    }*/
}
